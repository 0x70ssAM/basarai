{
  "name": "basarai-e2e",
  "private": true,
  "type": "module",
  "description": "Playwright E2E suite for Basar AI, targeting a live deployment (Render staging by default) or local dev via BASE_URL.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "dotenv": "^17.2.3"
  }
}
